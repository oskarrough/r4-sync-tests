Update the CHANGELOG.md file.

Use the changelog agent.

Radio4000 (r5) is a web app, not a library. Except the CLI. As such, we don't really have breaking changes. We do not want marketing announcement or sales pitches. Track user-facing changes.
